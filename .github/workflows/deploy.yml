name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
    - name: Verify files exist
      run: |
        echo "📁 Перевірка наявності файлів:"
        ls -la data/
        echo "✅ signals.json: $(wc -l < data/signals.json) рядків"
        echo "✅ history.json: $(wc -l < data/history.json) записів"
        
        # Створюємо .nojekyll якщо немає
        touch .nojekyll
      
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'
        
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
    
    - name: Log deployment
      run: |
        echo "🎉 Успішно задеплойлено на GitHub Pages!"
        echo "🌐 URL: ${{ steps.deployment.outputs.page_url }}"
        echo "🕐 Час деплою: $(date '+%H:%M:%S') UTC"
